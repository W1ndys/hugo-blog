{
  "framework": "hugo",
  "buildCommand": "git submodule update --init --recursive && hugo --minify --gc",
  "outputDirectory": "public",
  "build": {
    "env": {
      "HUGO_VERSION": "0.160.1"
    }
  }
}
